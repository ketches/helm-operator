apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-operator.fullname" . }}-test-connection"
  labels:
    {{- include "helm-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
